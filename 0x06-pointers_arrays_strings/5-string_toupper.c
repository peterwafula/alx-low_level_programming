#Finclude "main.h"

#include <ctype.h>

/**
  *string_toupper - converts string to upper
  *@s: string to be capitalized
  *Return: ccapitals
*/

char *string_toupper(char *s);
{
	return (toupper(s));
}
