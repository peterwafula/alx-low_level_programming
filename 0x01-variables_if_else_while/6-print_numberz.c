#include <stdio.h>

/**
  *main - This is the main entry point of my program.
  *arguments: This program taked no argument
  *Return: This program returns zero by default
*/

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
